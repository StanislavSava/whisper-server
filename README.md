# Whisper WebSocket Inference Server

A minimal real-time speech-to-text server powered by:

- FastAPI
- WebSockets
- whispercpp.py (bindings for whisper.cpp)
- GGML Whisper models

## Installation

```bash
pip install -r requirements.txt
```
